@model List<RutinaId>

@{
    Layout = "_LogLayout";
}

@section styles{
    <style>
/*        @@media (max-width: 839px) {
            .fechas {
                grid-template-columns: repeat(1, 1fr);
            }
        }*/

        @@media (min-width: 500px) {
            .fechas {
                grid-template-columns: repeat(2, 1fr) 100px;
            }
        }
    </style>
}


<h1 class="text-white text-center my-10 text-5xl">RUTINAS</h1>

<div class="mb-4">
    <form method="post">
        <div class="grid gap-6 fechas">
            <div class="relative w-full">
                <input type="date" name="fechainicio"
                       class="block p-2.5 w-full z-20 text-sm text-gray-900 bg-gray-50 rounded-lg border-l-gray-50 border-l-2 border border-gray-300 focus:ring-gray-500 focus:border-gray-500 dark:bg-gray-700 dark:border-l-gray-700  dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:border-gray-500"
                       placeholder="Busca un cliente..." />
            </div>
            <div class="relative w-full">
                <input type="date" name="fechafinal"
                       class="block p-2.5 w-full z-20 text-sm text-gray-900 bg-gray-50 rounded-lg border-l-gray-50 border-l-2 border border-gray-300 focus:ring-gray-500 focus:border-gray-500 dark:bg-gray-700 dark:border-l-gray-700  dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:border-gray-500"
                       placeholder="Busca un cliente..." />
            </div>
            <button type="submit" class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 mr-2 mb-2 dark:bg-blue-600 dark:hover:bg-blue-700 focus:outline-none dark:focus:ring-blue-800">Filtrar</button>
        </div>
    </form>
</div>

<h2 class="my-6 text-gray-400 text-2xl text-center">@ViewData["FILTRO"]</h2>

@foreach (RutinaId rutina in Model)
{
    <div class="m-3">
        <a href="/Entrenador/DetallesRutina?idrutina=@rutina.IdRutina" class="block w-full mb-3 p-6 bg-white border border-gray-200 rounded-lg shadow hover:bg-gray-100 dark:bg-gray-800 dark:border-gray-700 dark:hover:bg-gray-700">
            <h5 class="mb-2 text-2xl font-bold tracking-tight text-gray-900 dark:text-white">@rutina.Fecha.ToString("dd/MM/yyyy")</h5>
            <h5 class="mb-2 text-xl font-bold tracking-tight text-gray-900 dark:text-white">@rutina.Nombre</h5>
        </a>
    </div>
}